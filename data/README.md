# Data Directory

Use `orbit init-example --out data/example` to create a small deterministic dataset for local checks.

The example data are intentionally compact. Full ORBIT analyses should use derived disease signature tables and CMap/LINCS Level-5 landmark perturbation signatures prepared from the original public resources under their respective access terms.

